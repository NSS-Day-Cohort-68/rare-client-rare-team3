import { useEffect, useState } from "react"

export const TagsList = () => {
    const [allTags, setAllTags] = useState([])

    useEffect({
        
    }, [])

    return (
        <div>

        </div>
    )
}